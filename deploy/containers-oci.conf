[containers]
# Docker owns the aggregate cgroup for this single-concurrency runner. Nested
# rootless Podman cannot delegate child controllers without a systemd user
# manager, so keep its containers in the Compose service's cgroup.
cgroups = "disabled"
